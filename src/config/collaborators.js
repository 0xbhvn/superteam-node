const collaboratorStates = {
  REQUESTED: 'requested',
  ACCEPTED: 'accepted',
  REJECTED: 'rejected',
  BLOCKED: 'blocked',
  REMOVED: 'removed',
  DELETED: 'deleted',
  UNKNOWN: 'unknown',
};

module.exports = {
  collaboratorStates,
};
